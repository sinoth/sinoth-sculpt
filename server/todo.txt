
todo:
 [ ] consider sending server as one big packet instead of multiple sends for each piece

CREATE TABLE sculpture0 ( id integer primary key, username text, ip text, hostna
me text, checkout text, checkin text, data none );
